doctype html
html(lang="ru")
	head
		meta(charset='utf-8')
		block title
		meta(name="viewport" content="width=device-width, initial-scale=1.0")
		link(rel='stylesheet' href=`style/app${envSuffix}.css`)
	block content
